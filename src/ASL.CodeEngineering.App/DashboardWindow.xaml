<Window x:Class="ASL.CodeEngineering.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:ASL.CodeEngineering"
        Title="{x:Static res:Strings.DashboardTitle}" Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="PlanGrid" AutoGenerateColumns="True" Grid.Row="0"/>
        <DataGrid x:Name="SummaryGrid" AutoGenerateColumns="True" Grid.Row="1"/>
        <DataGrid x:Name="InsightGrid" AutoGenerateColumns="True" Grid.Row="2"/>
        <DataGrid x:Name="BenchmarkGrid" AutoGenerateColumns="True" Grid.Row="3"/>
    </Grid>
</Window>
